name: React Native Web checks

on:
  workflow_dispatch:
  push:
    branches:
      - main
      - master
    paths:
      - "react-native/**"
      - ".github/workflows/react_native_web_ci.yaml"
  pull_request:
    types:
      - opened
      - synchronize
      - reopened
      - ready_for_review
    paths:
      - "react-native/**"
      - ".github/workflows/react_native_web_ci.yaml"

permissions:
  contents: read

concurrency:
  group: react-native-web-${{ github.ref }}
  cancel-in-progress: true

jobs:
  web:
    if: github.event_name != 'pull_request' || github.event.pull_request.draft == false
    runs-on: ubuntu-latest
    timeout-minutes: 30

    steps:
      - name: Check out repository
        uses: actions/checkout@v6

      - name: Set up Node.js
        uses: actions/setup-node@v6
        with:
          node-version: "24.x"
          cache: npm
          cache-dependency-path: |
            react-native/package-lock.json
            react-native/example/package-lock.json

      - name: Install package dependencies
        working-directory: react-native
        run: npm ci

      - name: Install example dependencies
        working-directory: react-native/example
        run: npm ci

      - name: Run package checks
        working-directory: react-native
        run: npm run check

      - name: Build package
        working-directory: react-native
        run: npm run prepare

      - name: Validate package contents
        working-directory: react-native
        run: npm pack --dry-run

      - name: Install Playwright Chromium
        working-directory: react-native
        run: npx playwright install --with-deps chromium

      - name: Resolve and stage the published Web runtime
        working-directory: react-native/example
        run: npm run prepare-web

      - name: Build Web example
        working-directory: react-native/example
        run: npm run web:build

      - name: Run the headless WebAssembly smoke test
        working-directory: react-native
        run: npm run test:web
