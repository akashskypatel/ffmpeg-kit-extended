name: review16_custom_web_workspace
publish_to: none

environment:
  sdk: ">=3.12.0 <4.0.0"

workspace:
  - apps/app

hooks:
  user_defines:
    ffmpeg_kit_extended_flutter:
      type: base
      web: ../../../assets/wasm
