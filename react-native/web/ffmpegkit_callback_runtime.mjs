/**
 * React Native Web uses polling for log, statistics, and completion delivery.
 * This file is retained as a package asset for consumers that share the
 * generated Flutter-compatible Wasm bundle layout.
 */
export const callbackRuntime = Object.freeze({mode: 'polling'});
